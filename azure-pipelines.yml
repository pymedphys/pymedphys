jobs:
  - job: 'Linux'
    pool:
      vmImage: 'ubuntu-16.04'
    steps:
      - template: ./scripts/azure-pipelines/template.yml

  - job: 'Windows'
    pool:
      vmImage: 'vs2017-win2016'
    steps:
      - template: ./scripts/azure-pipelines/template.yml

  - job: 'macOS'
    pool:
      vmImage: 'macOS-10.13'
    steps:
      - template: ./scripts/azure-pipelines/template.yml
